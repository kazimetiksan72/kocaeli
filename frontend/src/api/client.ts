const API='/api/v1';
export class ApiError extends Error{constructor(public status:number,message:string,public fields:Record<string,string>={}){super(message)}}
export async function api<T>(path:string,options:RequestInit={}):Promise<T>{const token=localStorage.getItem('accessToken');const isForm=options.body instanceof FormData;const res=await fetch(API+path,{...options,headers:{...(!isForm?{'Content-Type':'application/json'}:{}),...(token?{Authorization:`Bearer ${token}`}:{}) ,...options.headers}});if(!res.ok){const body=await res.json().catch(()=>({message:'İşlem başarısız'}));throw new ApiError(res.status,body.message,Object.fromEntries((body.fieldErrors??[]).map((x:{field:string;message:string})=>[x.field,x.message])))}if(res.status===204)return undefined as T;return res.json() as Promise<T>}
