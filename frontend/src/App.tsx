import {useMemo,useState}from'react';import{ThemeProvider}from'@mui/material/styles';import{Navigate,Route,Routes}from'react-router-dom';import{makeTheme}from'./theme/theme';import LoginPage from'./pages/LoginPage';import Layout from'./components/Layout';import DashboardPage from'./pages/DashboardPage';import RequestsPage from'./pages/RequestsPage';import MapPage from'./pages/MapPage';import PlaceholderPage from'./pages/PlaceholderPage';
export default function App(){const[mode,setMode]=useState<'light'|'dark'>(()=>(localStorage.getItem('theme')as'light'|'dark')||'light');const theme=useMemo(()=>makeTheme(mode),[mode]);const logged=!!localStorage.getItem('accessToken');const toggle=()=>setMode(m=>{const n=m==='light'?'dark':'light';localStorage.setItem('theme',n);return n});return <ThemeProvider theme={theme}><Routes><Route path="/login" element={<LoginPage/>}/><Route element={logged?<Layout mode={mode} toggleMode={toggle}/>:<Navigate to="/login" replace/>}><Route index element={<Navigate to="/dashboard" replace/>}/><Route path="/dashboard" element={<DashboardPage/>}/><Route path="/map" element={<MapPage/>}/><Route path="/requests" element={<RequestsPage/>}/><Route path="/:section" element={<PlaceholderPage/>}/></Route></Routes></ThemeProvider>}

